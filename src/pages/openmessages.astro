---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Icon from '../components/Icons.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`OpenMessages | ${SITE_TITLE}`} description="Open-source Google Messages client for macOS with built-in AI tools. Read and send SMS/RCS from your Mac through your Android phone." />
	</head>
	<body>
		<Header />
		<main>
			<header class="page-header">
				<div class="app-icon">
					<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
					</svg>
				</div>
				<h1>OpenMessages</h1>
				<p class="page-description">Open-source Google Messages client for macOS with built-in AI tools. Read and send SMS/RCS from your Mac through your Android phone.</p>
				<div class="header-actions">
					<a href="https://github.com/MaxGhenis/openmessages/releases/latest" class="download-btn">
						<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path>
							<polyline points="7 10 12 15 17 10"></polyline>
							<line x1="12" y1="15" x2="12" y2="3"></line>
						</svg>
						Download for macOS
					</a>
					<a href="https://github.com/MaxGhenis/openmessages" class="github-btn">
						<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
							<path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
						</svg>
						View Source
					</a>
				</div>
			</header>

			<section class="features">
				<h2>Features</h2>
				<div class="feature-grid">
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="mail" size={32} duotone />
						</div>
						<h3>SMS and RCS</h3>
						<p>Full access to your Android text messages on macOS, including RCS group chats</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="robot" size={32} duotone />
						</div>
						<h3>AI-native with MCP</h3>
						<p>Built-in Model Context Protocol server lets Claude and other AI assistants read and send messages</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="lightning" size={32} duotone />
						</div>
						<h3>QR code pairing</h3>
						<p>Pairs with your Android phone in seconds via Google Messages, just like the web app</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="code" size={32} duotone />
						</div>
						<h3>Open source</h3>
						<p>MIT-licensed Go backend with a native Swift wrapper. Inspect, modify, and contribute</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="database" size={32} duotone />
						</div>
						<h3>Local-first</h3>
						<p>Messages stored in a local SQLite database. No cloud sync, no third-party servers</p>
					</div>
					<div class="feature-card">
						<div class="feature-icon">
							<Icon name="search" size={32} duotone />
						</div>
						<h3>Full-text search</h3>
						<p>Search across all your conversations instantly from the native macOS interface</p>
					</div>
				</div>
			</section>

			<section class="how-it-works">
				<h2>How it works</h2>
				<div class="install-steps">
					<div class="step">
						<span class="step-number">1</span>
						<div class="step-content">
							<h3>Launch OpenMessages</h3>
							<p>A QR code appears on screen</p>
						</div>
					</div>
					<div class="step">
						<span class="step-number">2</span>
						<div class="step-content">
							<h3>Scan from your Android phone</h3>
							<p>Open Google Messages → Settings → Device pairing → scan the QR code</p>
						</div>
					</div>
					<div class="step">
						<span class="step-number">3</span>
						<div class="step-content">
							<h3>Your messages appear</h3>
							<p>Conversations sync within seconds. Send and receive SMS/RCS natively on your Mac</p>
						</div>
					</div>
				</div>
			</section>

			<section class="mcp-section">
				<h2>Built for AI assistants</h2>
				<p>OpenMessages includes a <a href="https://modelcontextprotocol.io">Model Context Protocol</a> server that runs locally on your machine. This lets AI assistants like Claude:</p>
				<ul>
					<li>Search your message history</li>
					<li>Read conversations for context</li>
					<li>Send messages on your behalf</li>
					<li>React to messages</li>
				</ul>
				<p>All data stays on your machine. The MCP server connects to the same local SQLite database as the app.</p>
			</section>

			<section class="requirements">
				<h2>Requirements</h2>
				<ul>
					<li>macOS 14.0 (Sonoma) or later</li>
					<li>Android phone with Google Messages installed</li>
					<li>Phone and Mac on the same network for initial pairing</li>
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	main {
		max-width: 900px;
	}

	.page-header {
		text-align: center;
		margin-bottom: 3rem;
		padding-bottom: 2rem;
		border-bottom: 1px solid rgba(15, 23, 42, 0.08);
	}

	.app-icon {
		width: 80px;
		height: 80px;
		margin: 0 auto 1.5rem;
		background: linear-gradient(135deg, #1a6b4a 0%, #2d9a6a 100%);
		border: 2px solid #4ade80;
		border-radius: 16px;
		display: flex;
		align-items: center;
		justify-content: center;
		box-shadow: 0 0 30px rgba(74, 222, 128, 0.2);
	}

	.app-icon svg {
		width: 40px;
		height: 40px;
		color: white;
	}

	.page-header h1 {
		font-size: 3rem;
		margin-bottom: 0.75rem;
	}

	.page-description {
		font-size: 1.25rem;
		color: var(--text-secondary);
		max-width: 600px;
		margin: 0 auto 2rem;
	}

	.header-actions {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
	}

	.download-btn, .github-btn {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 0.875rem 1.5rem;
		border-radius: var(--radius-lg);
		text-decoration: none;
		font-weight: 600;
		font-size: 1rem;
		transition: all var(--duration-fast) var(--ease-out);
	}

	.download-btn {
		background: #4ade80;
		color: #0a0f0a;
	}

	.download-btn:hover {
		background: #22c55e;
		transform: translateY(-2px);
		box-shadow: 0 8px 30px rgba(74, 222, 128, 0.3);
	}

	.github-btn {
		background: var(--ink);
		color: white;
	}

	.github-btn:hover {
		background: var(--ink-soft);
		transform: translateY(-2px);
		box-shadow: var(--shadow-md);
	}

	section {
		margin-bottom: 3rem;
	}

	section h2 {
		font-size: 1.75rem;
		margin-bottom: 1.5rem;
	}

	.feature-grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
		gap: 1.25rem;
	}

	.feature-card {
		background: white;
		border: 1px solid rgba(15, 23, 42, 0.08);
		border-radius: var(--radius-lg);
		padding: 1.5rem;
		transition: all var(--duration-fast) var(--ease-out);
	}

	.feature-card:hover {
		transform: translateY(-2px);
		box-shadow: var(--shadow-md);
	}

	.feature-icon {
		width: 56px;
		height: 56px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: var(--amber-glow);
		border-radius: var(--radius-lg);
		margin-bottom: 1rem;
		color: var(--amber-dark);
		transition: all var(--duration-fast) var(--ease-out);
	}

	.feature-card:hover .feature-icon {
		background: var(--amber);
		color: var(--ink);
		transform: scale(1.05);
	}

	.feature-card h3 {
		font-size: 1.125rem;
		margin-bottom: 0.5rem;
	}

	.feature-card p {
		font-size: 0.9375rem;
		color: var(--text-secondary);
		line-height: 1.5;
	}

	.install-steps {
		display: flex;
		flex-direction: column;
		gap: 1.5rem;
	}

	.step {
		display: flex;
		align-items: flex-start;
		gap: 1.25rem;
	}

	.step-number {
		width: 36px;
		height: 36px;
		background: #4ade80;
		color: #0a0f0a;
		border-radius: 50%;
		display: flex;
		align-items: center;
		justify-content: center;
		font-weight: 700;
		flex-shrink: 0;
	}

	.step-content h3 {
		font-size: 1.125rem;
		margin-bottom: 0.25rem;
	}

	.step-content p {
		color: var(--text-secondary);
	}

	.mcp-section p {
		color: var(--text-secondary);
		line-height: 1.7;
		margin-bottom: 1rem;
	}

	.mcp-section a {
		color: var(--amber);
	}

	.mcp-section ul {
		list-style: none;
		padding: 0;
		margin-bottom: 1rem;
	}

	.mcp-section li {
		padding: 0.375rem 0;
		padding-left: 1.5rem;
		position: relative;
		color: var(--text-secondary);
	}

	.mcp-section li::before {
		content: "→";
		position: absolute;
		left: 0;
		color: #4ade80;
		font-weight: bold;
	}

	.requirements ul {
		list-style: none;
		padding: 0;
	}

	.requirements li {
		padding: 0.5rem 0;
		padding-left: 1.5rem;
		position: relative;
		color: var(--text-secondary);
	}

	.requirements li::before {
		content: "✓";
		position: absolute;
		left: 0;
		color: #4ade80;
		font-weight: bold;
	}

	@media (max-width: 768px) {
		.page-header h1 {
			font-size: 2.5rem;
		}

		.page-description {
			font-size: 1.125rem;
		}
	}
</style>
