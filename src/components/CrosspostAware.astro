---
/**
 * CrosspostAware - Wrapper component for content that differs between interactive and crosspost views
 *
 * Usage in MDX:
 * <CrosspostAware>
 *   <AICodingTimeline slot="interactive" />
 *   <Fragment slot="static">
 *     **Feb 24, 2025** — [Anthropic launches Claude Code](https://...)
 *     ...static timeline content...
 *   </Fragment>
 * </CrosspostAware>
 */

// Detect crosspost mode from URL
const isCrosspost = Astro.url.pathname.includes('/crosspost');
---

{isCrosspost ? (
  <slot name="static" />
) : (
  <slot name="interactive" />
)}

<style>
  .crosspost-static-content {
    /* Inherit parent styles */
  }
</style>
